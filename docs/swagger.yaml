openapi: 3.0.0
info:
  title: ADX GLOBAL BASE - API RESTful
  description: >
    Custom Swagger - Por Rafael Peres.
  version: 2.0.0

servers:
  - url: http://localhost:8080/adx-global-base/api
    variables:
      database:
        default: "SELECIONE O BANCO DE DADOS"
        enum:
          - "SELECIONE O BANCO DE DADOS"
          - "SQL Server"
          - "MySQL"
          - "SQLite"

components:
  securitySchemes:
    bearerAuth:
      type:              http
      scheme:            bearer
      bearerFormat:      JWT

  schemas:
    User:
      type:               object
      required:           [name, email, cpf]
      properties:
        name:             { type: string, example: "Leonardo Maia" }
        email:            { type: string, example: "leo.maia@gmail.com" }
        cpf:              { type: string, example: "36073089856" }
        rg:               { type: string, example: "33.331.981-8" }
        phone:            { type: string, example: "(19) 99999-8888" }
        address:          { type: string, example: "Rua dos Tucanos" }
        zipcode:          { type: string, example: "33.179-501" }
        number:           { type: number, example: 180 }
        district:         { type: string, example: "Centro" }
        city:             { type: string, example: "Campinas" }
        fu:               { type: string, example: "SP" }

    Customer:
      type:               object
      required:           [userId, name, email, cpf]
      properties:
        userId:           { type: string, example: "78EDBBC5F55946B299C624C5BFB612DF" }
        name:             { type: string, example: "Ricardo Cruz" }
        email:            { type: string, example: "ricruz@gmail.com" }
        cpf:              { type: string, example: "31014331420" }
        rg:               { type: string, example: "12.555.782-8" }
        age:              { type: number, example: 30 }
        phone:            { type: string, example: "(11) 99999-9999" }
        address:          { type: string, example: "Rua dos Sabiás" }
        zipcode:          { type: string, example: "03.811-344" }
        number:           { type: number, example: 1920 }
        district:         { type: string, example: "Bela Vista" }
        city:             { type: string, example: "São Paulo" }
        fu:               { type: string, example: "SP" }

    Order:
      type:               object
      required:           [customerId, description, totalValue, status]
      properties:
        customerId:       { type: string, example: "C61BE94913A545BF9F28B596D12973D2" }
        description:      { type: string, example: "Laptop Dell Inspiron" }
        totalValue:       { type: number, example: 1500.00 }
        status:           { type: string, example: "pending" }

    AuditResponse:
      type:               object
      required:           [status, detail, report_url]
      properties:
        status:
          type:           string
          example:        "success"
        detail:
          type:           string
          example:        "Documento de Auditoria criado com sucesso."
        report_url:
          type:           string
          example:        "/{project-root}/tests/report/"

paths:
  /api/setup/master:
    post:
      tags:               [Auth]
      summary:            "Disaster Recovery: Restaurar Usuário Master"
      requestBody:
        required:         true
        content:
          application/json:
            schema:
              type:       object
              required:   [secret]
              properties:
                secret:   { type: string, example: "ADX_MASTER_RECOVERY_2026" }
      responses:
        '201':            { description: "Criado" }
        '401':            { description: "Não autorizado" }

  /login:
    post:
      tags:               [Auth]
      summary:            "Realizar Login"
      requestBody:
        content:
          application/json:
            schema:
              type:       object
              properties:
                email:    { type: string, example: "master@adx-global-base.com" }
                password: { type: string, example: "123456" }
      responses:
        '200':
          description:    "OK"
          content:
            application/json:
              example:    { status: 200, data: { token: "JWT_TOKEN" } }

  /api/users:
    get:
      tags:           [Users]
      summary:        "Listar todos os usuários"
      security:       [{ bearerAuth: [] }]
      responses:      { '200': { description: "OK" } }
    post:
      tags:           [Users]
      summary:        "Cadastrar usuário"
      security:       [{ bearerAuth: [] }]
      requestBody:
        content:
          application/json:
            schema:   { $ref: '#/components/schemas/User' }
      responses:      { '201': { description: "Criado" } }

  /api/users/{id}:
    get:
      tags:           [Users]
      summary:        "Filtrar usuário por ID"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       id
          in:         path
          required:   true
          schema:     { type: string }
      responses:      { '200': { description: "OK" } }
    put:
      tags:           [Users]
      summary:        "Alterar dados do Usuário"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       id
          in:         path
          required:   true
          schema:     { type: string }
      requestBody:
        content:
          application/json:
            schema:   { $ref: '#/components/schemas/User' }
      responses:      { '200': { description: "Atualizado" } }
    delete:
      tags:           [Users]
      summary:        "Remover usuário"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       id
          in:         path
          required:   true
          schema:     { type: string }
      responses:      { '200': { description: "Excluído" } }

  /api/customers:
    get:
      tags:           [Customers]
      summary:        "Listar todos os clientes"
      security:       [{ bearerAuth: [] }]
      responses:      { '200': { description: "OK" } }
    post:
      tags:           [Customers]
      summary:        "Cadastrar cliente"
      security:       [{ bearerAuth: [] }]
      requestBody:
        content:
          application/json:
            schema:   { $ref: '#/components/schemas/Customer' }
      responses:      { '201': { description: "Criado" } }

  /api/customers/{id}:
    get:
      tags:           [Customers]
      summary:        "Filtrar por Customer ID"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       id
          in:         path
          required:   true
          schema:     { type: string }
      responses:      { '200': { description: "OK" } }
    put:
      tags:           [Customers]
      summary:        "Alterar dados do cliente"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       id
          in:         path
          required:   true
          schema:     { type: string }
      requestBody:
        content:
          application/json:
            schema:   { $ref: '#/components/schemas/Customer' }
      responses:      { '200': { description: "Atualizado" } }
    delete:
      tags:           [Customers]
      summary:        "Remover cliente"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       id
          in:         path
          required:   true
          schema:     { type: string }
      responses:      { '200': { description: "Excluído" } }

  /api/customers/user/{userId}:
    get:
      tags:           [Customers]
      summary:        "Listar clientes por Usuário"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       userId
          in:         path
          required:   true
          schema:     { type: string }
      responses:      { '200': { description: "OK" } }

  /api/orders:
    get:
      tags:           [Orders]
      summary:        "Listar todos os pedidos"
      security:       [{ bearerAuth: [] }]
      responses:      { '200': { description: "OK" } }
    post:
      tags:           [Orders]
      summary:        "Criar novo pedido"
      security:       [{ bearerAuth: [] }]
      requestBody:
        content:
          application/json:
            schema:   { $ref: '#/components/schemas/Order' }
      responses:      { '201': { description: "Criado" } }

  /api/orders/{id}:
    get:
      tags:           [Orders]
      summary:        "Filtrar pedido por ID"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       id
          in:         path
          required:   true
          schema:     { type: string }
      responses:      { '200': { description: "OK" } }
    put:
      tags:           [Orders]
      summary:        "Alterar status ou dados do pedido"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       id
          in:         path
          required:   true
          schema:     { type: string }
      requestBody:
        content:
          application/json:
            schema:   { $ref: '#/components/schemas/Order' }
      responses:      { '200': { description: "Atualizado" } }
    delete:
      tags:           [Orders]
      summary:        "Cancelar/Excluir pedido"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       id
          in:         path
          required:   true
          schema:     { type: string }
      responses:      { '200': { description: "Cancelado" } }

  /api/orders/customer/{customerId}:
    get:
      tags:           [Orders]
      summary:        "Listar pedidos por Cliente"
      security:       [{ bearerAuth: [] }]
      parameters:
        - name:       customerId
          in:         path
          required:   true
          schema:     { type: string }
      responses:      { '200': { description: "OK" } }